The library for pointcloud segmentation should go here. It must therefore
include the pointcloud lib for point acquisition.


If you want to use segmantation under Ubunto, you must first install the
following libraries:

For Berkeley DB:
-libdb4.8++-dev
